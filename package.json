{
  "name": "mishwari-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --filter=@mishwari/passenger-web --filter=@mishwari/driver-web --concurrency=5",
    "dev:passenger": "turbo run dev --filter=@mishwari/passenger-web",
    "dev:driver": "turbo run dev --filter=@mishwari/driver-web",
    "build:packages": "pnpm --filter @mishwari/types build && pnpm --filter @mishwari/utils build && pnpm --filter @mishwari/api build && pnpm --filter @mishwari/ui-primitives build && pnpm --filter @mishwari/ui-web build && pnpm --filter '@mishwari/features-*' build",
    "build:apps": "turbo run build --filter='./apps/*'",
    "build": "pnpm build:packages && pnpm build:apps",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "clean": "turbo run clean && find packages -name 'dist' -type d -exec rm -rf {} +"
  },
  "devDependencies": {
    "@svgr/webpack": "^8.1.0",
    "turbo": "^1.10.0",
    "typescript": "^5.0.0"
  }
}